apiVersion: v1
kind: Service
metadata:
  name: devpath-platform-svc
  labels:
    app: devpath-platform-svc
spec:
  selector:
    app: devpath-platform-svc
  ports:
    - port: 8080
      targetPort: 8080
